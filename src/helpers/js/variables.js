// variables de la cantidad de paginas existentes en la API
const numMax = 501;
const numMin = 1;

const numMaxTR = 143;
const numMinTR = 1;

const numMaxSS = 238;
const numMinSS = 1;

export { numMax, numMin, numMaxTR, numMinTR, numMaxSS, numMinSS }