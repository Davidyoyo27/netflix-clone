// variables con rangos de cantidad de paginas de endpoints de peliculas
const numMax500=500
const numMax290=290
const numMax200=200
const numMax100=100
const numMax40=40
const numMax20=20
const numMax10=10

export { numMax500, numMax290, numMax200, numMax100, numMax40, numMax20, numMax10 };